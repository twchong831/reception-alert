version: '3.8'

services:
  visitor-server:
    build: .
    container_name: visitor-server
    ports:
      - '8803:8803'
    volumes:
      - /volume1/visitor-app/data:/app/data
    environment:
      - PORT=8803
      - DATA_DIR=/app/data
    restart: unless-stopped
